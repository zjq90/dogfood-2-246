<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.it.mapper.DoumailMapper">

    <resultMap id="DoumailResultMap" type="com.it.model.Doumail">
        <id column="doumail_id" property="doumailId"/>
        <result column="from_user_id" property="fromUserId"/>
        <result column="to_user_id" property="toUserId"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="is_read" property="isRead"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        doumail_id, from_user_id, to_user_id, content, status, is_read, create_time
    </sql>

    <select id="selectConversationList" resultMap="DoumailResultMap">
        SELECT DISTINCT
            CASE WHEN from_user_id = #{userId} THEN to_user_id ELSE from_user_id END as target_user_id,
            MAX(doumail_id) as last_doumail_id
        FROM doumail
        WHERE (from_user_id = #{userId} AND status != 1)
           OR (to_user_id = #{userId} AND status != 2)
        GROUP BY target_user_id
        ORDER BY last_doumail_id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectDoumailDetail" resultMap="DoumailResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM doumail
        WHERE ((from_user_id = #{userId} AND to_user_id = #{targetUserId} AND status != 1)
           OR (from_user_id = #{targetUserId} AND to_user_id = #{userId} AND status != 2))
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <insert id="insertDoumail" parameterType="com.it.model.Doumail" useGeneratedKeys="true" keyProperty="doumailId">
        INSERT INTO doumail (from_user_id, to_user_id, content, status, is_read, create_time)
        VALUES (#{fromUserId}, #{toUserId}, #{content}, 0, 0, NOW())
    </insert>

    <update id="markAsRead">
        UPDATE doumail 
        SET is_read = 1 
        WHERE from_user_id = #{fromUserId} AND to_user_id = #{toUserId} AND is_read = 0
    </update>

    <update id="deleteDoumail">
        UPDATE doumail 
        SET status = #{status}
        WHERE doumail_id = #{doumailId} 
          AND ((from_user_id = #{userId} AND status = 1) 
            OR (to_user_id = #{userId} AND status = 2))
    </update>

    <select id="selectUnreadCount" resultType="int">
        SELECT COUNT(*) FROM doumail WHERE to_user_id = #{userId} AND is_read = 0 AND status != 2
    </select>

    <select id="selectConversationCount" resultType="long">
        SELECT COUNT(DISTINCT 
            CASE WHEN from_user_id = #{userId} THEN to_user_id ELSE from_user_id END
        ) FROM doumail
        WHERE (from_user_id = #{userId} AND status != 1)
           OR (to_user_id = #{userId} AND status != 2)
    </select>

    <select id="selectDoumailCount" resultType="long">
        SELECT COUNT(*) FROM doumail
        WHERE ((from_user_id = #{userId} AND to_user_id = #{targetUserId} AND status != 1)
           OR (from_user_id = #{targetUserId} AND to_user_id = #{userId} AND status != 2))
    </select>

    <!-- 兼容方法 -->
    <select id="selectDoumailDetailCount" resultType="long">
        SELECT COUNT(*) FROM doumail
        WHERE ((from_user_id = #{userId} AND to_user_id = #{targetUserId} AND status != 1)
           OR (from_user_id = #{targetUserId} AND to_user_id = #{userId} AND status != 2))
    </select>

    <update id="updateStatus">
        UPDATE doumail 
        SET status = #{status} 
        WHERE from_user_id = #{fromUserId} AND to_user_id = #{toUserId}
    </update>

    <delete id="deleteById">
        DELETE FROM doumail WHERE doumail_id = #{doumailId} AND (from_user_id = #{userId} OR to_user_id = #{userId})
    </delete>

</mapper>